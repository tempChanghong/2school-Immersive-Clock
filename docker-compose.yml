version: "3.8"

services:
  immersive-clock:
    build: .
    ports:
      - "8080:80"
    restart: unless-stopped
    labels:
      - "com.immersive-clock.description=Immersive Clock - Elegant time management app"
